<?php 

$root_path  = dirname(__FILE__);

require_once(dirname(__FILE__)."/core/boot.php");
require_once(CORE_PATH."/components/loader.php");

$app  = new Application();
$data = parse_ini_file(dirname(__FILE__)."/config/config.ini", true);

define('PROJECT', strtolower(str_replace(" ","_", $data['project']['name'])));
define('ENVIRONMENT', 'development');

// Add all desired paths to the task paths array;

$task_paths = array(
	"tasks",
	"script"
	);


?>